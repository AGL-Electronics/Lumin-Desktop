fn main() {
    tauri_build::build()
}

/* struct AppManifest {
    version: String,
    authors: Vec<String>,
    website: String,
    website_label: String,
}

fn generate_app_metadata() -> AppManifest {
  // read github metadata abut commit history and generate metadata
  // for the app

} */
